# Application
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgres://postgres:password@localhost:5432/devflow

# (Optional individual fields — used by drizzle-kit if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=devflow
