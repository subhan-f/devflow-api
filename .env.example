# Application
NODE_ENV=development
PORT=3000

# CORS — production only; space-separated list of allowed origins
# CORS_ORIGIN=https://app.example.com

# Database — required by both the app and drizzle-kit
DATABASE_URL=postgres://postgres:password@localhost:5432/devflow
